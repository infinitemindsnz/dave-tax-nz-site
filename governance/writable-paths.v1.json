{
  "schemaVersion": 1,
  "contractId": "dave-tax-nz-site-content.v1",
  "files": [
    {
      "path": "src/data/site.yaml",
      "fields": [
        {
          "proposalField": "public_phone",
          "operationKind": "public_phone_patch",
          "jsonPointers": [
            "/contact/rows/0/value",
            "/contact/rows/0/href",
            "/hero/ctas/1/href"
          ],
          "writeMode": "atomic",
          "comment": "COUPLED SET — WRITE ATOMICALLY OR REFUSE. The public phone number is denormalised across three pointers: the human-readable display string, the contact-row tel: href, and the hero call-to-action tel: href. A patch that writes any subset leaves the site self-contradictory. THESE HAVE ALREADY DRIFTED UPSTREAM: src/App.jsx shipped display \"+64 21 021 68888\" (12 digits after the +64 grouping) against href \"tel:+64210216888\" (11 digits), and src/data/site.yaml preserves that bug deliberately rather than guessing the correct number. A publisher MUST NOT auto-reconcile the drift; correcting it is a client-confirmed content decision that arrives as an explicit phone patch covering all three pointers in one candidate."
        }
      ]
    }
  ],
  "preconditions": [
    {
      "comment": "Two of the permitted pointers address array elements by index, so a reorder of `contact.rows` or `hero.ctas` would silently retarget this policy at the email row, the office row or the booking button. Before applying any patch, the publisher MUST assert every pointer/value pair below against the BASE revision and refuse the candidate on any mismatch. These are identity assertions, not content the policy may write.",
      "path": "src/data/site.yaml",
      "assertions": [
        { "jsonPointer": "/contact/rows/0/label", "equals": "Call Dave directly" },
        { "jsonPointer": "/hero/ctas/1/label", "equals": "Call Dave" },
        { "jsonPointer": "/hero/ctas/1/variant", "equals": "secondary" }
      ]
    }
  ],
  "documentedOperations": [
    {
      "operationKind": "public_hours_patch",
      "permitted": false,
      "comment": "WITHDRAWN IN VERSION 1 — WAS PERMITTED, NOW REFUSED. This operation kind previously appeared in `files` above as proposalField `consultation_availability` mapped to the single pointer /contact/action/title. That mapping was WRONG AND DESTRUCTIVE and has been removed. The value at /contact/action/title is \"Free 15-minute initial consultation\" — a call-to-action for the client's booking funnel, NOT an opening-hours statement. An approved hours value such as \"Monday to Friday, 9am-5pm\" would therefore have OVERWRITTEN the client's free-consultation CTA with an hours string, and the verify-build gate would have reported the publication verified, because that gate proves the approved bytes landed at the approved pointer — it does not and cannot prove that the pointer means what the proposal field is named. No approval stage catches this either: an approver approves an hours STRING, never the pointer it lands on. The prior entry acknowledged the mismatch and argued the CTA was \"the narrowest honest equivalent\" availability statement. That reasoning is withdrawn: a call-to-action is not an availability record, and a near-miss pointer is more dangerous than no pointer at all, because it converts a request the contract should refuse into a silent, verified content loss. Because `files` is an exhaustive allowlist and this contract's `default` is deny (enforced by the publisher's policy loader), removing the entry means an hours request is now REFUSED as out-of-contract. That is the intended behaviour: fail closed, and let the client be asked for a real opening-hours record rather than have their CTA destroyed on their behalf.",
      "wouldTarget": {
        "comment": "Recorded so the next reader does not re-derive the same wrong mapping from the same scarcity of candidate pointers. There is no correct target in this repository today. Enumerated as a warning, not as a latent grant.",
        "paths": [],
        "wouldNotTarget": [
          {
            "path": "src/data/site.yaml#/contact/action/title",
            "reason": "THE REMOVED MAPPING. Holds the booking call-to-action title (\"Free 15-minute initial consultation\"), rendered as the heading of the contact action block alongside ctaLabel and ctaHref. It is conversion copy, not availability. Writing hours here destroys the CTA and leaves the adjacent \"Book a consultation\" button labelled for an action the heading no longer describes."
          },
          {
            "path": "src/data/site.yaml#/contact/rows/*",
            "reason": "The three contact rows are phone, email and the Auckland office address — each a label/value/href triple. None is an hours field, and row indices are already load-bearing for the phone coupled set under `preconditions`. Appending an hours row would be a schema change and a new visible page element, not a patch."
          },
          {
            "path": "src/data/pages.yaml",
            "reason": "The migrated `contact` page's \"Contact Dave\" list carries \"Auckland Mobile\", \"Email\" and \"WhatsApp available\" — verbatim client copy, and still no hours. pages.yaml is not a writable path under any operation kind in this contract regardless."
          }
        ]
      },
      "preconditionsBeforeEnabling": [
        "A REAL opening-hours field. src/data/schema.ts is a zod strictObject and defines no hours key anywhere in the site model, so there is currently no pointer an hours value can address without inventing one. Adding it is a content-model change requiring the client's actual opening hours, and it makes hours a newly rendered element on the page — a design and content decision, not a policy entry.",
        "A materialiser for that new pointer in the publisher's code-owned MATERIALISERS table. The contract says which pointers a field owns; the publisher says what canonical bytes each pointer receives, and a contract pointer with no materialiser fails the policy load closed.",
        "Version 2 of this contract, with its own review. Version 1 must not be stretched to cover an operation whose only previous target was the wrong one.",
        "A semantic check binding each proposalField to the MEANING of its pointers, not merely to their existence. This bug was reachable precisely because every structural gate passed: the pointer existed, the grammar validated, the approval was genuine, the bytes changed as approved. Nothing in the chain asserted that a field named for availability writes to a field that holds availability."
      ]
    },
    {
      "operationKind": "article_publish",
      "permitted": false,
      "comment": "DOCUMENTED, NOT AUTHORIZED. `files` above is the exhaustive allowlist and this entry is deliberately not in it: version 1 permits the four hours/phone pointers in src/data/site.yaml and nothing else. This block exists so that the shape of an operation this repository refuses is written down in public rather than left to a future implementer's inference — the same stance the reference contract takes toward `article_publish`. Migrating eighteen articles into src/content/articles did not widen this policy by a single path. Those files are human-owned: they were migrated by hand and are edited by hand. Widening the allowlist to cover them is a separate, deliberate decision requiring the preconditions below, a version 2 of this contract, and its own review.",
      "wouldTarget": {
        "comment": "What the operation WOULD address if it were ever permitted. Enumerated so a reviewer can see the blast radius of enabling it, not so a publisher can act on it.",
        "paths": [
          {
            "path": "src/content/articles/<slug>.md",
            "mode": "create-only",
            "comment": "One new Markdown file per article. Create-only: modifying or deleting an existing article is a different operation with a different risk profile — an edit can silently alter or remove an attribution on already-published reporting — and would need its own documented entry. `<slug>` must satisfy the frontmatter `slug` pattern and must equal the filename, which src/lib/articles.ts already enforces at build time."
          }
        ],
        "constrainedBy": [
          "governance/schemas/article-frontmatter.v1.schema.json",
          "governance/markdown-policy.v1.json"
        ],
        "wouldNotTarget": [
          {
            "path": "src/data/articles.yaml",
            "reason": "The curated homepage list is a separate content source with its own editorial judgement — the client chose those eight records and their order. Publishing an article does not entitle a writer to promote it onto the homepage. That is a distinct decision and would be a distinct operation."
          },
          {
            "path": "src/data/pages.yaml",
            "reason": "Includes the `articles-advice` category filter list, which is the client's own chosen set and order (\"Uncategorized\" included, because WordPress recorded it). An article introducing a new category must NOT silently append to that list; a filter the client did not choose is a content change, not a side effect."
          },
          {
            "path": "src/data/site.yaml",
            "reason": "Holds the four article-surface labels in `articlePages`, including the two that frame the source-credit block. Those are site chrome, not article content."
          },
          {
            "path": "src/data/media-manifest.json",
            "reason": "Adding a manifest entry means adding a binary under public/assets/wp/. Asset ingestion is a separate capability with its own supply-chain surface; the markdown policy's requirement that every image URL already resolve here is a gate, and letting the same operation write the gate would defeat it."
          },
          {
            "path": "src/styles.css",
            "reason": "Byte-frozen client stylesheet. Refused to every operation kind regardless of approval."
          },
          {
            "path": "src/styles-article.css",
            "reason": "Renderer-owned. Bound by renderer_sha256, not a content source."
          },
          {
            "path": "dist/**",
            "reason": "Build output. Derived, never written directly."
          }
        ]
      },
      "approvalPolicy": {
        "comment": "governance/approval-policy.v1.json already binds this operation kind to artifact class `article` and the ordered stages below, and governance/schemas/candidate-manifest.v1.schema.json already refuses any other stage list for it. Both are structural descriptions. Neither is a grant: a fully-approved article_publish candidate is still refused here, because this file does not list a path it may write.",
        "requiredApprovalStages": ["legal_sign_off", "publication_approval"]
      },
      "preconditionsBeforeEnabling": [
        "A separate Article Engine contract defining canonical author and reviewer identity. The frontmatter has no author or reviewedBy field; on a repost the JSON-LD author is derived from `attribution.source`, which is a publisher name, not a bound identity.",
        "Source expiry and re-review state. Tax law changes and this corpus makes dated statutory claims (Section CB 4 of the Income Tax Act 2007, the 10-day statutory demand window). Nothing here records when a claim was last verified.",
        "An executing verifier for governance/markdown-policy.v1.json. The policy is written down and checked by nobody — no code in this repository parses an article body against it.",
        "An attribution-integrity check that refuses any diff touching a body attribution line or a frontmatter `attribution` object without an explicit approved attribution change. Twelve of the eighteen posts carry another newsroom's credit; an operation that can write article bodies can silently rewrite an authorship claim.",
        "A schema-drift check binding governance/schemas/article-frontmatter.v1.schema.json to src/content.config.ts, and governance/schemas/pages.v1.schema.json to src/data/schema.ts. The field-for-field requirement is currently stated and unenforced.",
        "A slug-collision and route-identity check at candidate time rather than build time. src/lib/articles.ts fails the build on a duplicate or filename/frontmatter mismatch, which refuses the candidate late and only if the publisher runs a build."
      ]
    }
  ],
  "default": "deny"
}
